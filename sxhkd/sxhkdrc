# reinicia o daemon do sxhkd
super + Escape
	pkill -USR1 -x sxhkd

# navegadores
# Google Chrome
super + c
    google-chrome
# Firefox
super + f
    firefox

# editores
# emacs
super + e
    emacsclient -c -a "emacs"
# vim
super + v
    st vim
# RStudio
super + r
    rstudio

# trava a tela
super + l
    slock

# terminal (comando adicional ao alt + shift + <enter> do dwm
super + Return
    st 

# captura de tela (fonte: https://github.com/GabrielTWMlinux/Scripts/blob/main/screenshot)
super + shift + s
    ~/.config/screenshot.sh

# volume
XF86AudioRaiseVolume
    pactl set-sink-volume 0 +5%

XF86AudioLowerVolume
    pactl set-sink-volume 0 -5%

XF86AudioMute
    pactl set-sink-mute 0 toggle

# brilho da tela
# descubra o nome do monitor via: xrandr | grep ' connected' | awk '{print $1}'
XF86MonBrightnessUp
    ~/.config/brightness.sh + eDP-1

XF86MonBrightnessDown
    ~/.config/brightness.sh - eDP-1
